# =====================================================================
# Root requirements — combined install for the full stack.
#   pip install -r requirements.txt
#
# Splits into:
#   - backend  (FastAPI · RDKit · admet-ai · meeko)
#   - frontend (Flask + gunicorn)
#
# Non-Python docking dependency:
#   AutoDock Vina must be installed as a system/Conda binary named `vina`
#   for real docking. On macOS use: conda install -c conda-forge vina
#   Without it, the backend intentionally runs docking in stub mode.
# =====================================================================
-r backend/requirements.txt
-r flask_frontend/requirements.txt
